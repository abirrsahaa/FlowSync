function App() {
  return (
    <main className="flex min-h-full flex-col items-center justify-center gap-3 p-8 text-center">
      <p className="font-mono text-xs uppercase tracking-[0.2em] text-app-ink-muted">
        FlowSync · Frontend Scaffold
      </p>
      <h1 className="text-2xl font-semibold text-app-navy">
        Setup complete — see BUILD_SESSIONS.md
      </h1>
      <p className="max-w-md text-sm text-app-ink-muted">
        Tailwind tokens, fonts, path aliases, and dependencies are wired up.
        Screens are built session by session per the plan.
      </p>
    </main>
  )
}

export default App
